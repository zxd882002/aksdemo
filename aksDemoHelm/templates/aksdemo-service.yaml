apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.aksdemo.name }}
spec:
  selector:
    app: {{ .Values.aksdemo.name }}
  ports:
    - name: http
      port: {{ .Values.aksdemo.service.port }}
      targetPort: 80
 
