apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.weatherForecastWeb.name }}-{{ .Values.weatherForecastWeb.version }}
spec:
  replicas: {{ .Values.weatherForecastWeb.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.weatherForecastWeb.name }}
      version: {{ .Values.version | quote }}
  template:
    metadata:      
      labels:
        app: {{ .Values.weatherForecastWeb.name }}
        version: {{ .Values.version | quote }}
    spec:
      containers:
        - name: {{ .Values.weatherForecastWeb.name }}          
          image: "{{ .Values.weatherForecastWeb.image.repository }}:{{ .Values.weatherForecastWeb.version }}"
          imagePullPolicy: {{ .Values.weatherForecastWeb.image.pullPolicy }}
          ports:
            - containerPort: 80
              protocol: TCP
          env:
            - name: ASPNETCORE_URLS
              value: http://+:80
          livenessProbe:
            httpGet:
              path: /
              port: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80         