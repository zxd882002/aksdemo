apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.weather-forecast-web.name }}
spec:
  selector:
    app: {{ .Values.weather-forecast-web.name }}
  ports:
    - name: http
      port: {{ .Values.weather-forecast-web.service.port }}
      targetPort: 80
 
