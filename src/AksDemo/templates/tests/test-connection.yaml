apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aksDemo.fullname" . }}-test-connection"
  labels:
    {{- include "aksDemo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aksDemo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
